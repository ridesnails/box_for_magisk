name: Test Display

on:
  workflow_dispatch:
  push:
    branches:
      - simple

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Test step
      run: |
        echo "✅ 工作流显示测试"
        echo "当前分支: ${{ github.ref_name }}"
        echo "触发事件: ${{ github.event_name }}"
